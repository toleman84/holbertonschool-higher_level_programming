import doctest
doctest.testfile("0-add_integer.txt")

The ``example`` module
======================

Using ``factorial``
-------------------

This is an example text file in reStructuredText format.  First import
``factorial`` from the ``example`` module:

    >>> from 0-add_integer.txt import add_integer

Now use it:

    >>> print(add_integer(1, 1))
    2
